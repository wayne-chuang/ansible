AWS:
  SOURCE:
    ACCESS_KEY: "{{ VAULT_AWS.SOURCE.ACCESS_KEY }}"
    SECRET_KEY: "{{ VAULT_AWS.SOURCE.SECRET_KEY }}"
    REGION: ap-southeast-1
  DEST:
    ACCESS_KEY: "{{ VAULT_AWS.DEST.ACCESS_KEY }}"
    SECRET_KEY: "{{ VAULT_AWS.DEST.SECRET_KEY }}"
    REGION: ap-southeast-1
    ACCOUNT_ID: "{{ VAULT_AWS.DEST.ACCOUNT_ID }}"
